apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tln-db-operator-system

commonLabels:
  app.kubernetes.io/name: tln-db-operator

resources:
  - ../manager
  - ../rbac
  - ../crd

# Patches applied to the manager Deployment.
patches:
  - path: manager_config_patch.yaml
    target:
      kind: Deployment
      name: tln-db-operator-controller-manager
