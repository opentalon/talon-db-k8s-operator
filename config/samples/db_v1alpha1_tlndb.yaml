apiVersion: db.tlndb.io/v1alpha1
kind: TlnDB
metadata:
  name: tlndb-sample
  labels:
    app.kubernetes.io/name: tln-db
    app.kubernetes.io/instance: tlndb-sample
spec:
  image:
    repository: ghcr.io/opentalon/tln-db
    tag: latest
    pullPolicy: IfNotPresent
  replicas: 1

  # Rendered into a ConfigMap and mounted at /etc/tlndb/config.yaml.
  config:
    dbPath: /data/tlndb.bbolt
    tcp: ":9899"
    http: ":8080"

  # Inject configuration/secrets. tlndb-server reads TLNDB_* env vars.
  # env:
  #   - name: TLNDB_TCP
  #     value: ":9899"
  envFrom:
    - configMapRef:
        name: tlndb-sample-extra-config
        optional: true
    - secretRef:
        name: tlndb-sample-secrets
        optional: true

  storage:
    persistence:
      enabled: true
      size: 1Gi
      accessModes:
        - ReadWriteOnce
      # storageClassName: standard

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: "1"
      memory: 512Mi

  networking:
    service:
      type: ClusterIP
    ingress:
      enabled: false
    networkPolicy:
      enabled: false

  observability:
    metrics:
      enabled: true
      port: 9090
      serviceMonitor:
        enabled: false
        interval: 30s

  availability:
    podDisruptionBudget:
      enabled: false
    horizontalPodAutoscaler:
      enabled: false
