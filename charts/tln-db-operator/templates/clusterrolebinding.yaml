{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "tln-db-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "tln-db-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "tln-db-operator.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "tln-db-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
